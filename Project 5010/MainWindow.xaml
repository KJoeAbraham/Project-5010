<Window x:Class="Project_5010.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project_5010"
        mc:Ignorable="d"
        Title="Fitness Tracker" Height="450" Width="500">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Log Workout"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Workout Type"/>
            <ComboBox x:Name="WorkoutTypeCombo" Width="200" SelectedIndex="0">
                <ComboBoxItem Content="Cardio"/>
                <ComboBoxItem Content="Strength"/>
                <ComboBoxItem Content="Flexibility"/>
            </ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Duration (minutes)"/>
            <TextBox x:Name="DurationTextBox" Width="200"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,10">
            <TextBlock Text="Notes"/>
            <TextBox x:Name="NotesTextBox"
                     Width="300"
                     Height="80"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"/>
        </StackPanel>

        <Button Grid.Row="4"
                x:Name="SaveWorkoutButton"
                Content="Save Workout"
                Width="140"
                Height="35"
                Margin="0,10,0,10"
                Click="SaveWorkoutButton_Click"/>

        <TextBlock Grid.Row="5"
                   x:Name="StatusTextBlock"
                   Foreground="Green"
                   FontWeight="SemiBold"
                   Margin="0,10,0,0"/>
    </Grid>
</Window>