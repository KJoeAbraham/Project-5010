<UserControl x:Class="Project_5010.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:Project_5010.Models"
             Background="Transparent">

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#1C1C1E" CornerRadius="20" Padding="24" Margin="0,0,24,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="MOMENTUM LIBRARY" FontSize="13" FontWeight="Bold" Foreground="#6D4AFF" Margin="0,0,0,2"/>
                <TextBlock Grid.Row="1" Text="Exercises" FontSize="36" FontWeight="Black" Foreground="White" Margin="0,0,0,16"/>

                <Border Grid.Row="2" Background="#2C2C2E" BorderBrush="#6D4AFF" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock x:Name="SuggestionTitleText" Text="Today's Focus" FontWeight="Bold" Foreground="#6D4AFF" Margin="0,0,0,4"/>
                        <TextBlock Text="Based on your settings and training split." Foreground="#8E8E93" FontSize="12" Margin="0,0,0,10"/>
                        <ItemsControl x:Name="SuggestionItemsControl">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding StringFormat='• {0}'}" Foreground="White" FontSize="14" Margin="0,2,0,0"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Grid Grid.Row="3" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border Background="#2C2C2E" CornerRadius="12" Padding="14,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="🔍" Foreground="#8E8E93" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="16"/>
                            <TextBox x:Name="SearchTextBox" Grid.Column="1" TextChanged="SearchTextBox_TextChanged" 
                                     Background="Transparent" Foreground="White" BorderThickness="0" FontSize="16" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <ComboBox x:Name="CategoryFilterCombo" Grid.Column="1" SelectionChanged="FilterCombo_SelectionChanged" 
                              Margin="16,0,0,0" Width="130" Height="42" FontSize="15" Padding="10,0"
                              Background="#2C2C2E" Foreground="Black" BorderThickness="0">
                        <ComboBoxItem Content="All"/>
                        <ComboBoxItem Content="Push"/>
                        <ComboBoxItem Content="Pull"/>
                        <ComboBoxItem Content="Legs"/>
                        <ComboBoxItem Content="Cardio"/>
                    </ComboBox>
                </Grid>

                <ListBox x:Name="ExercisesListView" Grid.Row="4" SelectionChanged="ExercisesListView_SelectionChanged" 
                         Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0,0,0,10"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="CardBorder" Background="#2C2C2E" CornerRadius="12" Padding="16" Cursor="Hand">
                                            <ContentPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="CardBorder" Property="Background" Value="#3A3A3C"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="CardBorder" Property="Background" Value="#6D4AFF"/>
                                                <Setter TargetName="CardBorder" Property="BorderBrush" Value="White"/>
                                                <Setter TargetName="CardBorder" Property="BorderThickness" Value="1"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:Exercise}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="17" Foreground="White"/>
                                    <TextBlock Text="{Binding Muscle, StringFormat='Target: {0}'}" Foreground="#D1D1D6" FontSize="13" Margin="0,4,0,0"/>
                                </StackPanel>
                                <Border Grid.Column="1" Background="#1C1C1E" CornerRadius="6" Padding="10,6" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Equipment}" Foreground="#6D4AFF" FontSize="12" FontWeight="Bold"/>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="#1C1C1E" CornerRadius="20" Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="MNTM TRACKER" FontSize="13" FontWeight="Bold" Foreground="#6D4AFF" Margin="0,0,0,2"/>
                <TextBlock Grid.Row="1" Text="Performance" FontSize="36" FontWeight="Black" Foreground="White" Margin="0,0,0,20"/>

                <Border Grid.Row="2" Background="#2C2C2E" CornerRadius="16" Padding="20" Margin="0,0,0,24">
                    <StackPanel>
                        <TextBlock Text="Selected Exercise" Foreground="#8E8E93" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                        <Border Background="#1C1C1E" CornerRadius="8" Margin="0,0,0,16">
                            <TextBox x:Name="PRExerciseTextBox" Background="Transparent" Foreground="White" BorderThickness="0" Padding="14" FontSize="15" IsReadOnly="True" Text="Select an exercise from the left..."/>
                        </Border>

                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <TextBlock Text="Weight (lbs/kg)" Foreground="#8E8E93" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                                <Border Background="#1C1C1E" CornerRadius="8">
                                    <TextBox x:Name="PRWeightTextBox" Background="Transparent" Foreground="White" BorderThickness="0" Padding="14" FontSize="15"/>
                                </Border>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                <TextBlock Text="Date" Foreground="#8E8E93" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                                <Border Background="#1C1C1E" CornerRadius="8">
                                    <DatePicker x:Name="PRDatePicker" Background="Transparent" Foreground="White" BorderThickness="0" Padding="8,4" Height="42"/>
                                </Border>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="Notes / Reps" Foreground="#8E8E93" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                        <Border Background="#1C1C1E" CornerRadius="8" Margin="0,0,0,20">
                            <TextBox x:Name="PRNotesTextBox" Background="Transparent" Foreground="White" BorderThickness="0" Padding="14" FontSize="15"/>
                        </Border>

                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="SavePRButton" Content="Log PR" Background="#6D4AFF" Foreground="White" FontWeight="Bold" FontSize="15" Padding="20,12" BorderThickness="0" Cursor="Hand" Click="SavePRButton_Click"/>
                            <Button x:Name="DeletePRButton" Content="Delete" Background="#FF3B30" Foreground="White" FontWeight="Bold" FontSize="15" Padding="20,12" Margin="12,0,0,0" BorderThickness="0" Cursor="Hand" Visibility="Collapsed" Click="DeletePRButton_Click"/>
                        </StackPanel>
                        <TextBlock x:Name="PRStatusText" Margin="0,16,0,0" FontWeight="Bold" FontSize="14"/>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Row="3">
                    <TextBlock Text="Trophy Room (History)" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,16"/>
                    
                    <ListView x:Name="PRListView" SelectionChanged="PRListView_SelectionChanged" Background="Transparent" BorderThickness="0" Foreground="White">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Exercise" Width="140" DisplayMemberBinding="{Binding ExerciseName}"/>
                                <GridViewColumn Header="Weight" Width="60" DisplayMemberBinding="{Binding Weight}"/>
                                <GridViewColumn Header="Date" Width="100" DisplayMemberBinding="{Binding Date, StringFormat={}{0:MMM dd, yyyy}}"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>